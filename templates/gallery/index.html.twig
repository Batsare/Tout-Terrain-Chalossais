{% extends 'base.html.twig' %}

{% block container %}
<div class="row">
    <div class="col-12">
        <div class="row">
            {#<div class="h1 rounded bg-success text-white pl-2 pr-2">Album photos</div>#}
            <div class="col-12 col-md-6">
                <div class="rounded-right bg-success text-white pr-2">
                    <p class="h1 text-right">Album photos</p>
                </div>
            </div>
        </div>
        <div class="row justify-content-center">
            {#<p>{{ gallery.createdAt|date("d/m/Y") }} - </p>#}
            {% for gallery in galleries %}

                <div class="col-12 text-center m-2"><a class=" h4 text-success" href="{{ path('gallery_view', { 'id' : gallery.id } ) }}">{{ gallery.createdAt|date("F Y") }} - {{ gallery.name }}</a></div>

            {% endfor %}
        </div>


        {#<div class="row justify-content-center">#}
            {#<ul class="list-group col-lg-6">#}
                {#{% for gallery in galleries %}#}

                    {#<li class="list-group-item d-flex justify-content-between align-items-center"><a href="{{ path('gallery_view', { 'id' : gallery.id} ) }}">#}

                            {#{{ gallery.name }}#}
                            {#<span class="badge badge-success badge-pill">{{ gallery.createdAt|date("d/m/Y") }}</span>#}
                        {#</a>#}
                    {#</li>#}

                {#{% endfor %}#}
            {#</ul>#}

        {#</div>#}
    </div>
</div>
{% endblock %}